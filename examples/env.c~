#include "lists.h"

int main(int ac, char **av, char **env)
{
	int k;
	int i = 0;
	extern char **environ;

	char *buf;

	buf = strdup(*environ);

	while (*buf)
	{
		printf("%s\n", buf);
	}

	return (0);
}
